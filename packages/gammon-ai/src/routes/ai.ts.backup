// src/routes/ai.ts - TEMPORARILY DISABLED
import express from 'express';

const router = express.Router();

// Temporarily disable AI routes for build
router.get('/', (req, res) => res.json({ success: true, message: 'AI API temporarily disabled' }));
router.post('/', (req, res) => res.json({ success: true, message: 'AI API temporarily disabled' }));

/*
// src/routes/ai.ts
import express from 'express';
import { authMiddleware } from '../middleware/authMiddleware';
import { makeAIMove, getAIMoveSuggestion, getAIAnalysis } from '../controllers/aiController';

const router = express.Router();

// All AI routes require authentication
router.use(authMiddleware);

// POST /api/ai/:gameId/move - Make AI move
router.post('/:gameId/move', makeAIMove);

// GET /api/ai/:gameId/suggestion - Get AI move suggestion
router.get('/:gameId/suggestion', getAIMoveSuggestion);

// GET /api/ai/:gameId/analysis - Get AI position analysis
router.get('/:gameId/analysis', getAIAnalysis);
*/

export default router;
